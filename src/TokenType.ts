export enum TokenType {
    EOF,
    LITERAL,
    IGNORE_RULE,
    OR,
    ARROW,
    RULE,
    LEFT_PAREN,
    RIGHT_PAREN,
    PLUS,
    STAR,
    SEMI_COL,
    ONE_LINE_COMM,
    MULTI_LINE_COMM
}
